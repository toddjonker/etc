# Shell options for ZSH

  setopt auto_cd      # If unknown command is a directory, cd into it.
  setopt correct      # Autocorrect misspelled commands
unsetopt correct_all  # Autocorrect misspelled arguments; very annoying
